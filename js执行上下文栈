1.顺序执行？
  一般是顺序执行，但是，js引擎并非一行一行的分析和执行代码，而是一段一段的分析执行。

2.当js引擎遇到什么样的代码才会进行准备工作呢？
  js的可执行代码的类型有三种，一是全局代码、二是函数代码，三是eval代码，当遇到这三种代码时，就进行准备工作。
  专业一些的话，就是遇到“执行上下文（execution context）”的时候。
  js引擎创建了执行上下文栈（Execution context stack ECS）来管理执行上下文。
  
